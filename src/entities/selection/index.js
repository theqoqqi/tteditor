import {
    addAllToSelection,
    addToSelection,
    clearSelection,
    removeFromSelection,
    removeAllFromSelection,
    selectionSlice,
    selectSelectedMapNodes,
    setSelection,
    toggleSelection
} from './model/selectionSlice';

export {
    selectionSlice,
    setSelection,
    clearSelection,
    selectSelectedMapNodes,
    addToSelection,
    addAllToSelection,
    removeFromSelection,
    removeAllFromSelection,
    toggleSelection,
};
