import {
    triggerListSlice,
    selectSelectedTrigger,
    setSelectedTrigger,
} from './model/triggerListSlice';

export {
    triggerListSlice,
    selectSelectedTrigger,
    setSelectedTrigger,
};
