import styles from './RightSidebar.module.css';
import React from 'react';
import classNames from 'classnames';

function RightSidebar({}) {
    return (
        <div className={classNames(styles.rightSidebarContent)}>

        </div>
    );
}

export default RightSidebar;